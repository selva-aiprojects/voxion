services:
  # Voxion Assistant Engine (API & WebSockets)
  - type: web
    name: voxion-backend
    runtime: node
    plan: starter # Optimized for AI/WebSocket workloads
    buildCommand: npm install && npm run build --workspace=apps/backend
    startCommand: node apps/backend/dist/main
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: OPENAI_API_KEY
        sync: false # Set manually in Render Dashboard
      - key: ENCRYPTION_KEY
        sync: false # Set manually in Render Dashboard
      - key: DATABASE_URL
        sync: false # Set manually in Render Dashboard
    healthCheckPath: /
    rootContext: .
